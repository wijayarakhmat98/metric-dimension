#!/usr/bin/env bash

set -euo pipefail

export NAME='batch-all'

export VERTICES=''
export VERTICES="${VERTICES} 2"
export VERTICES="${VERTICES} 3"
export VERTICES="${VERTICES} 4"
export VERTICES="${VERTICES} 5"
export VERTICES="${VERTICES} 6"
export VERTICES="${VERTICES} 7"
export VERTICES="${VERTICES} 8"
export VERTICES="${VERTICES} 9"
export VERTICES="${VERTICES} 10"
export VERTICES="${VERTICES} 15"
export VERTICES="${VERTICES} 20"
export VERTICES="${VERTICES} 25"
export VERTICES="${VERTICES} 30"
export VERTICES="${VERTICES} 35"
export VERTICES="${VERTICES} 40"
export VERTICES="${VERTICES} 45"
export VERTICES="${VERTICES} 50"

./benchmark-exec/main
