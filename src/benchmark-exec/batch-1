#!/usr/bin/env bash

set -euo pipefail

export NAME='batch-1'

export VERTICES=''
export VERTICES="${VERTICES} 2"
export VERTICES="${VERTICES} 3"
export VERTICES="${VERTICES} 4"
export VERTICES="${VERTICES} 5"
export VERTICES="${VERTICES} 6"
export VERTICES="${VERTICES} 7"
export VERTICES="${VERTICES} 8"
export VERTICES="${VERTICES} 9"
export VERTICES="${VERTICES} 10"

./benchmark-exec/main
