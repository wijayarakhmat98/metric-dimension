#!/usr/bin/env bash

set -euo pipefail

export OUTPUT='benchmark-result'

mkdir -p "${OUTPUT}"

for source in benchmark-result/*.txt; do
	file="${source%%.txt}"
	target="${file}.simplified"
	echo "Sorting ${file}..."
	cat "${source}"  | ./process simplify > "${target}"
done
