#!/usr/bin/env bash

set -euo pipefail

export OUTPUT='benchmark-result'

mkdir -p "${OUTPUT}"

for source in benchmark-result/*.sorted; do
	file="${source%%.sorted}"
	target="${file}.csv"
	echo "Creating CSV from ${file}..."
	cat "${source}"  | ./process csv > "${target}"
done
